{
  "name": "procon-site",
  "private": true,
  "description": "Pro Con LLC — Cloudflare Worker (static assets + contact API)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
